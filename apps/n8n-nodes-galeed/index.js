// pacote de community nodes do n8n — os entrypoints reais estão declarados no bloco "n8n"
// do package.json (dist/credentials/*, dist/nodes/*). Este arquivo existe por convenção.
module.exports = {};
